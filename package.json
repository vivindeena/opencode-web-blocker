{
  "name": "opencode-web-blocker",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/chrome-extension",
    "packages/zen-extension",
    "packages/opencode-plugin"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "start": "bun --filter '*' start",
    "check": "bun --filter '*' check",
    "type-check": "bun run check",
    "publish:plugin": "bun scripts/publish.ts plugin",
    "publish:server": "bun scripts/publish.ts server"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
